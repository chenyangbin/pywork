# 工程目录：c:\Users\bin\OneDrive\share\pywork\day02\基础语法关键字.py
# 创建日期: 2019.02.11
# 工程目标：关键字了解
# 创建作者：binyang
# -*- coding:utf-8 -*-


#关键字及其作用
'''
False               def                 if                  raise
None                del                 import              return
True                elif                in                  try
and                 else                is                  while
as                  except              lambda              with
assert              finally             nonlocal            yield
break               for                 not                 
class               from                or                  
continue            global              pass  

作用：
False，True ：布尔值
None :  表示空值，有自己的数据类型NoneType
and，or，not：逻辑与、或、非
as ：给引入的模块取新的名字
assert ：声明某个表达式必须为真，编程中若该表达式为假就会报错AssertionError
break，continue，pass：控制语句和占位词,break,跳出本次循环，继续下次循环。
if，else，elif，for，while：结构语句
class：用来定义类
def，return： 用来定义函数，从函数返回
del：删除列表中不需要的对象，删除定义过的对象
try，except，finally：try结构
from，import：从模块中导入函数，调用时可省略模块名
lambda：匿名函数
in：判断对象是否在列表中
is：判断两个变量是否指向同一对象
global：定义全局变量，即便实在函数体内定义的global变量也全局可用
nolocal：
with：
raise：
yield：生成器用
'''
